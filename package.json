{
  "name": "react-node-js",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server"
  }
}
