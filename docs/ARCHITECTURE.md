# Architecture Notes

## Current Implementation

- `client/`: React + Vite basic frontend scaffold.
- `server/`: Express API scaffold with `GET /api/health` endpoint.

## Next Steps

1. Add API modules (auth/users).
2. Add database configuration.
3. Add tests and CI pipeline.
